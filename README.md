# linux Commands

## This is a list of linux commands 
```ssh
 - `ls` list dir
 - `chmod` change modification
 - `useradd adds user
 - `groupadd` adds groups
 - `usermod` modify user
 - `su` switch user
```
